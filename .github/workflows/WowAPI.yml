
name: WOW API TRAINING
on:  
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  authenticate-bnet:
    runs-on: windows-latest
    steps:
    
      - name: Check out repo
        uses: actions/checkout@v2

      - name: Authenticate with BNet
        id: bnet-auth
        run: |
          
          $access_token =  ./Scripts/GetBnetToken.ps1 ${{secrets.WOW_CLIENT_ID}} ${{secrets.WOW_CLIENT_SECRET}}
          Write-Host "accToken $access_token"
          echo '::set-output name=access-token::${{access_token}}'

      - name: Get Characters from Server
        run: |
          $access_token = ${{steps.bnet-auth.outputs.access-token}}
          Write-Host $access_token

          